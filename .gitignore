# Banco de dados SQLite (não versionar — cada ambiente tem o seu)
admin/data/*.db
admin/data/*.db-shm
admin/data/*.db-wal

# Config com credenciais (cada ambiente tem o seu)
admin/config.php

# Arquivos temporários
*.zip
*.log
.DS_Store
Thumbs.db
